apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: jager-rd-default
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/jager-rd-default: "true"
  podSelector:
    matchLabels:
      io.kompose.network/jager-rd-default: "true"
